const {chromium}=require('playwright'),assert=require('assert');
(async()=>{const b=await chromium.launch({executablePath:'C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe'});try{const p=await b.newPage({viewport:{width:1100,height:1000}});const errors=[];p.on('pageerror',e=>errors.push(e.message));await p.emulateMedia({reducedMotion:'no-preference'});await p.goto('http://127.0.0.1:8770/blackout/',{waitUntil:'networkidle'});await p.click('#go-bot');const hit=await p.evaluate(()=>{BO.Match.stop();const s=BO.Core.create(0);s.side=0;s.ps[0].x=0;s.ps[0].y=0;s.ps[1].x=3;s.ps[1].y=3;s.lit=0;window.hitState=s;BO.UI.reset();BO.Core.act(s,0,['s',3,3]);BO.UI.render(BO.Core.view(s,0));BO.UI.events(s.ev,true);const c=document.querySelector('.cell.foe');return {pose:getComputedStyle(c,'::after').backgroundImage,mask:getComputedStyle(c.querySelector('.glowfx')).maskImage,liquid:c.querySelectorAll('.liquid-mass,.liquid-rim').length};});assert(hit.pose.includes('kid-hit.png'));assert(hit.mask.includes('kid-hit.png'));assert.equal(hit.liquid,2);await p.evaluate(()=>{hitState.turn++;BO.UI.render(BO.Core.view(hitState,0));});assert.equal(await p.locator('.cell.foe').count(),0);assert.equal(await p.locator('.cell.hit-reaction.foe').count(),0);await p.evaluate(()=>BO.UI.reset());await p.waitForTimeout(900);assert.equal(await p.locator('.hit-reaction,.liquid-mass,.liquid-rim').count(),0);await p.goto('http://127.0.0.1:8770/blackout/tools/art-motion.html',{waitUntil:'networkidle'});await p.click('#hit');await p.evaluate(()=>document.getAnimations().forEach(a=>{a.pause();a.currentTime=170}));await p.screenshot({path:process.env.SP+'/hit-170ms.png'});await p.evaluate(()=>document.getAnimations().forEach(a=>a.play()));await p.waitForTimeout(1100);assert.equal(await p.locator('.hit-reaction').count(),0);await p.emulateMedia({reducedMotion:'reduce'});await p.click('#hit');assert.equal(await p.locator('.liquid-mass').count(),0);assert.deepEqual(errors,[]);console.log('PASS hit pose and silhouette, liquid layers, immediate re-hide, reset cancellation, reduced motion');}finally{await b.close()}})().catch(e=>{console.error(e);process.exit(1)});